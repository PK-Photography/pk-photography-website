import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pkphotography.appname',
  appName: 'PK-Photography',
  webDir: 'public'
};

export default config;
